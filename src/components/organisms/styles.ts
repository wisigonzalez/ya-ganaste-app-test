import {StyleSheet} from 'react-native';

import {resPixel} from '../../utils';

export const ListStyles = StyleSheet.create({
  container: {
    height: resPixel(350),
    marginLeft: resPixel(10),
    marginRight: resPixel(10),
  },
});
